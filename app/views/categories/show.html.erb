<div class="row" id="show-box">
  <div class="col-md-6 col-md-offset-3">
    <div class="box">
      	<div class="panel panel-default">
      		<div class="panel-body">
      			<h1><%= @category.name %></h1>
      			<p>Publish at
				        <%= @category.created_at.strftime('%b %d, %Y') %></p>

      			<%= link_to 'Back', categories_path, class: "btn btn-info" %>				
      		</div>
		</div>
    </div>
  </div>
</div>
